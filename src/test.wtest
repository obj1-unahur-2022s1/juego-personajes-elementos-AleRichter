import jugadoresPersonajes.*
import cosas.*
import armas.*

test "FLoki ataca el castillo con la ballesta y 10 flechas" {
	
    floki.cambiarArma(ballesta)
	floki.encontrarElemento(castillo)
    assert.equals(154 - ballesta.nivelDePotencia(), castillo.nivelDefensa())
}